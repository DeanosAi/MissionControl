# Journal - Wednesday, 8 April 2026

## ChatGPT and Codex orchestration setup

### Goal
Set up Scot as a high-level orchestrator that delegates heavier execution work to ChatGPT and Codex through OpenClaw OAuth, reducing Claude cost while keeping coding and research horsepower on Dean's ChatGPT plan.

### System architecture
| Role | Model | Responsibility |
| --- | --- | --- |
| Orchestrator | Claude Opus 4.6 | Task decomposition, routing, decisions, messaging |
| Primary execution | GPT-5.4 / Codex | Coding, research, data processing, file generation |
| First fallback | Kimi K2.5 | Backup if Codex is unavailable |
| Survival mode | Local LLM | Degraded fallback if cloud access fails |

### What got done
1. ChatGPT OAuth authenticated with two stored profiles.
2. Installed the missing `@openai/codex` and `acpx` CLIs globally.
3. Enabled the OpenClaw `acpx` plugin.
4. Verified the end-to-end `acpx codex exec` path.
5. Documented routing rules so Scot defaults to Codex for coding and research work.

### Known issue
The `sessions_spawn` wrapper with `runtime: "acp"` still reports that the ACP backend is unavailable even though direct `acpx codex exec` works. The next attempt is after a full machine restart and the pending OpenClaw update.

## Mission Control operational pass

### What changed
1. Upgraded the homepage hero and routing presentation so the AI setup is clearer and more attractive at a glance.
2. Completed the Task Board Archived flow with an explicit Archived column, deterministic 7-day auto-archive based on `completedAt`, and newest-first archived ordering.
3. Fixed lingering encoding issues in UI text, task data, team data, and journal files.
4. Added a Windows Scheduled Task startup path for Mission Control so it can relaunch independently of OpenClaw gateway restarts.
5. Updated Mission Control tasks, team metadata, and docs to reflect the final pass.

### Startup approach
Mission Control now has:

- `scripts/start-mission-control.ps1` to run the production Next.js server from this repo
- `scripts/install-autostart.ps1` to register a `Mission Control` Scheduled Task at user logon with restart-on-failure settings

This keeps Mission Control separate from OpenClaw gateway lifecycle and is the simplest reliable Windows-native option for this project.

## Mission Control restart note

The dev server had previously been restarted for local access on `http://localhost:3000`. After this pass, the production startup path is documented and scriptable; a restart is required for the updated build to be served by `next start`.

## Mission Control VPS hosting preparation - Phase 1

### Goal
Prepare Mission Control for later VPS hosting without deploying yet, while preserving the current local development and localhost workflow.

### What changed
1. Added shared runtime config so data, docs, memory, host, port, and base URL can be overridden later through environment variables.
2. Replaced hard-coded JSON storage paths with a shared storage layer that still defaults to local repo `data/` and `docs/`.
3. Added `scripts/prepare-runtime.mjs`, production helper npm scripts, and a new `/api/health` endpoint for startup and readiness checks.
4. Added Phase 1 VPS notes covering persistence paths, reverse proxy expectations, basic auth/security posture, and the later deployment checklist.
5. Updated Mission Control task and team data to reflect the new hosting-preparation phase.

### Result
Mission Control is now ready for the next step of VPS deployment planning, but no server has been configured or touched yet. The local experience on `http://localhost:3000` remains the default.

## Still open

- Content Hub Mac DMG Gatekeeper issue remains parked pending proper notarization.
- `sessions_spawn` ACP wrapper remains parked until after restart and OpenClaw update.
